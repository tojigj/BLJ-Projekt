generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:Alejor-4278@localhost:3306/sitzungszimmerdb"
}

model user {
  user_id  Int     @id
  username String? @db.VarChar(35)
  password String? @db.VarChar(30)
}

model stockwerke {
  stockwerk_id   Int     @id
  stockwerk_nr   Int?
  stockwerk_name String? @db.VarChar(30)
}
