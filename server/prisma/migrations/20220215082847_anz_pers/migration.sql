-- CreateTable
CREATE TABLE `anzUser` (
    `anzUser` INTEGER NOT NULL,
    `anzUserId` INTEGER NOT NULL AUTO_INCREMENT,

    PRIMARY KEY (`anzUserId`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
